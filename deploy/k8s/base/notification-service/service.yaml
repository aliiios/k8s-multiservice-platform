apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: platform
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - port: 4002
      targetPort: 4002
